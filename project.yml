# gspm project.yml <https://gitlab.com/godot-stuff/gs-project-manager/wikis/schema>
name: gs-logger
path: .
version: 3.1.0
default_type: git
godot:
  location: $310$
project:
  url: none
  
assets:

pre_install:

post_install:

pre_build:

post_build:
